# Changelog

All notable changes to this project will be documented in this file.

The format is based on [Keep a Changelog](https://keepachangelog.com/en/1.1.0/),
and this project adheres to [Semantic Versioning](https://semver.org/spec/v2.0.0.html).

## [Unreleased]

### Added

- 新增黑色风格的 `meeting` 主图标 SVG 资产 `docs/design/meeting-logo-black.svg`，保留 `me` 两个小写字母和底部聚光灯效果，适用于移动 APP、微信和网站 Logo。
- 新增根目录 `scripts/` 发布脚本目录，包含 `start.sh`、`stop.sh`、`restart.sh`、`status.sh`、`update.sh`、`upload.sh` 和 `crontab.sh`，并纳入标准发布包。
- 新增 `docker-compose.yml`、前端 Nginx 配置和 coturn 配置草案，为 Docker 化运行的后端、前端和 TURN 服务提供统一编排。
- 新增标准发布流程 `make linux`、`make pack`、`make upload` 和 `make publish`，最终产物固定为 `meeting_${commit}.tar.gz` 与 `latest.txt`。
- 初始化 `Meeting` 项目骨架，明确 P2P 优先、TURN 兜底、会议数据会后清理的架构方向。
- 新增架构 ADR、后端服务入口、SQLite 审计/偏好存储和前端基础会议控制台骨架。
- 新增基础 WSS 信令通道，支持房间欢迎消息、在线状态广播、聊天消息、能力申请/授权和 SDP/ICE 消息转发。
- 新增前端 1v1 WebRTC P2P 建链能力，支持本地媒体采集、远端流渲染和基于信令的 SDP/ICE 交换。
- 新增前端屏幕共享与本地录制缓存流程，支持浏览器内存缓存、下载保存和手动丢弃。
- 新增助理授权、白板协作、就位确认和会中临时聊天/纪要运行态，支持会内查看并在会议结束后自动清理。
- 新增基于 `RTCPeerConnection.getStats()` 的基础审计统计上报与前端摘要展示，向后端周期性上报延迟、丢包、帧率和码率。
- 新增临时会议纪要查询接口 `GET /api/meetings/{meetingID}/minutes`，用于读取会中聊天、白板和就位确认快照。
- 新增昵称修改接口和前端入口，修改昵称后会自动写入临时聊天记录与会议纪要。
- 新增参会者离开会议入口和主持人保护规则，主持人在仍有其他参会者时必须显式结束会议，避免房间进入无主持人状态。
- 新增临时纪要本地导出入口，支持把会中纪要、聊天、白板数量和就位确认摘要导出为文本文件。
- 新增 `docs/design/` 设计资产目录，包含可复用的 HTML/CSS 示意页和桌面端、移动端实际渲染图。
- 新增 `Makefile`，统一提供 `build`、`run-backend`、`run-frontend` 和 `clean` 命令。
- 新增 `docs/issues/README.md`，整理最初需求与当前实现之间的功能差距和遗留问题清单。
- 新增根目录 `.gitignore`，忽略 Go/Vite 构建产物、依赖目录、日志、SQLite、本地系统文件和 `docs/design` 渲染图片。
- 新增 `README.zh-CN.md`，提供简体中文项目说明。
- 新增 `docs/design/meeting-entry-preview.html` 和 `docs/design/meeting-entry-preview.css`，补齐创建 / 加入会议状态的可复用设计源码。
- 新增 `docs/design/meeting-auth-preview.html`、`docs/design/meeting-auth-preview.css`、`docs/design/meeting-room-preview.html` 和 `docs/design/meeting-room-preview.css`，用于预览下一版黑色风格的登录 / 入会 / 会中主舞台设计。
- 新增 `docs/design/20260325-product-ui-rollout.md`，记录新版产品化前端 UI 的落地范围、兼容实现、风险与回滚思路。
- 新增 `docs/design/meeting-room-preview.html`、`docs/design/meeting-room-preview.css` 和 `docs/design/20260327-room-fullscreen-menu-layout.md`，用于评审会中单页全屏主舞台与主持人 / 参会者双菜单分层方案。
- 新增会议信息分享二维码和加入会议扫码按钮，支持通过二维码快速回填会议号和密码，提升局域网联调效率。
- 新增前端结构化调试日志：浏览器控制台输出 JSON 日志，并在会中页提供“复制前端日志”入口，便于排查创建会议、入会、信令、结束会议和扫码问题。
- 新增前端日志接收接口 `POST /api/client-logs`，用于接收浏览器侧关键 JSON 日志并写入现有后端日志体系。
- 新增 `docs/api/` 接口文档目录，使用 OpenAPI 和 WebSocket 协议文档补齐 REST 与信令接口契约说明。
- 新增 `docs/proposals/20260331-media-reliability-and-identity-plan.md`，整理媒体可靠性与身份体系两条主线的实施计划、范围、风险与验证思路。
- 新增 `docs/proposals/20260331-docker-release-and-update-flow.md`，整理 Docker 化运行、根目录发布脚本、原地更新流程与标准打包发布方案。
- 新增 `docs/issues/README.md` 的 20260331 方案执行拆解，补充媒体可靠性与身份体系两条主线的可执行 issue 列表。
- 新增 `docs/deploy/coturn.md` 和 `web/.env.example`，补齐 TURN / coturn 的部署样例和前端环境配置示例。

### Changed

- `make publish` / `make upload` 的职责边界按标准发布流程重新收敛：`publish` 现在严格按 `clean -> linux -> pack -> upload` 线性执行，`upload` 只校验并上传现成产物，不再隐式触发重新构建或重新打包；上传阶段同时对齐 `gobot` 的进度条逻辑，优先使用 `pv`，否则回退到 `curl --progress-bar`。
- 发布包中的运维脚本改为在解压后直接平铺到当前目录，不再额外保留 `scripts/` 子目录；仓库内的 `scripts/` 源码仍保留不变，并补齐了根目录与 `scripts/` 两种布局下的路径兼容逻辑。
- Makefile 现在同时支持本地开发构建和 Docker 化发布打包，`build` 继续面向本地开发，`linux` / `pack` / `upload` / `publish` 面向标准发布流程。
- 固化权限模型：`participant` 默认仅有文字聊天权限，其他能力需主持人授权。
- 固化录制策略：录屏/录音默认本地录制，不上传服务器。
- 会中聊天改为服务端临时保存，断线重连后仍可通过房间快照恢复，会议结束后统一删除。
- 会议控制台补充了昵称编辑、离会和纪要导出操作，减少仅有后端能力但前端不可用的缺口。
- 重写 README，补齐模块职责表、需求实现状态矩阵、关键接口说明和设计资源索引。
- 前端构建输出和可控缓存目录统一收敛到 `build/`，便于集中管理构建产物和临时文件。
- README 新增 Issue 清单入口，便于后续跟踪待开发功能和技术债务。
- `README.md` 调整为英文默认文档，并补充中英文切换入口，适配面向全球的开源仓库常见做法。
- `docs/design` 目录约定调整为：HTML/CSS 预览源码和渲染脚本入库，示意图图片按本地产物处理并加入忽略规则。
- `docs/design/render-previews.sh` 改为基于脚本目录解析路径，避免依赖开发者本机绝对路径。
- `docs/design/meeting-console-preview.html` 和样式文件补齐了就位确认、本地录制、事件流等已实现的会中区域，和当前前端页面结构保持一致。
- `docs/design/README.md` 和 `docs/design/render-previews.sh` 补充下一版产品预览入口，支持单独渲染登录 / 入会页和会中舞台页的桌面端、移动端图片。
- 重做 `docs/design/meeting-room-preview.html` 和 `docs/design/meeting-room-preview.css`，让会中预览直接铺满浏览器窗口并更接近参考截图中的深色单屏会议房间，同时补齐无边框头像、仅图标麦克风状态、悬浮编辑昵称和仿截图底部工具栏。
- 继续收紧 `docs/design/meeting-room-preview.html` 和 `docs/design/meeting-room-preview.css` 的细节，修正头像下方麦克风状态与昵称的居中对齐，移除工具栏核心按钮下方的多余符号，并删除 `元宝纪要` 入口。
- 进一步调整会中预览的头像信息布局，将昵称编辑按钮改为悬浮定位，避免它影响昵称的居中位置，并增强昵称下方麦克风图标的可见性。
- 修复会中预览头像下方麦克风标识的 SVG 样式，让它复用工具栏同款图标并缩小显示，避免出现黑色实心图形。
- 清理会中预览页左上角多余状态图标，保留更简洁的会议标题区域，并让顶部菜单、底部工具栏和部分弹层具备可点击的预览反馈，不再是静态页面。
- 恢复会中预览页右上角的网络状态和分享入口，避免误删顶栏右侧操作区。
- 将会中预览页的网络状态和分享入口移动到会议详情左侧，并移除会议详情左边残留的两个装饰符号。
- 调整会中预览页顶栏左侧的排列顺序为 `会议详情 → 网络状态 → 分享`，让入口层级更清晰。
- 给会中预览页所有按钮弹出的窗口统一补上右上角关闭按钮，方便直接收起弹窗。
- 将弹窗右上角关闭按钮改成更接近 macOS 的小红色圆形样式，并缩小尺寸。
- 统一会中预览子窗口的关闭逻辑：右上角关闭、重复点击同一触发按钮和 `ESC` 都可以关闭，空白区域点击不再关闭，切换子窗口时会先关闭当前窗口再打开目标窗口。
- 清理会中预览页左上角的窗口控制和额外状态按钮，保留更接近截图的极简会议标题区域。
- 收紧会中预览页底部工具栏，移除左侧聊天输入和举手控件，并让“应用 / 结束会议 / 主持人工具 / 会议工具”弹窗优先贴近触发按钮，其他弹窗继续居中显示。
- 将“结束会议”子窗口改成更接近参考截图的按钮式菜单，并让结束按钮在菜单打开时切换为 `取消`，便于直接收起。
- 将结束会议操作卡片改为按工具栏顶部单独计算底边位置，并收紧按钮样式，尽量确保三项操作在视口内完整可见。
- 继续收紧会中预览页的交互细节：主持人默认昵称改为 `主持人`、其他未改名参会者默认显示为 `匿名用户`，顶栏补充 `设置` 和 `进入全屏` 入口，网络状态窗口移除关闭按钮，聊天改为右侧抽屉并在打开时同步压缩主舞台宽度，同时让结束会议菜单贴近底部工具栏显示。
- 继续优化会中预览页的全屏入口：鼠标悬停时显示 `进入全屏模式` / `退出全屏模式` 提示，进入全屏后同步切换图标样式与提示文案。
- 修复会中预览里结束会议菜单的定位裁切问题：该菜单改为固定定位并继续贴合工具栏顶部，避免在靠近底部时只显示一部分。
- 调整登录预览页为全窗口展示，左侧仅保留 `meeting` 标识，右侧邮箱和密码提示改为输入框占位文案 `请输入邮箱` 和 `请输入密码`。
- 进一步收紧登录预览页内容：右侧登录框改为居中悬浮卡片，仅保留邮箱登录标题、邮箱与密码输入框、获取临时验证码按钮、登录 / 加入会议按钮以及右下角的忘记密码入口。
- 修正登录预览页的居中方式：登录卡片在登录态下改为全屏浮层居中，不再受右侧栏宽度约束。
- 清理登录预览页右侧残留的独立空白分栏，登录态现在只有全屏背景和居中浮层卡片。
- 进一步精简登录预览页卡片，移除标题与字段说明文字，补充微信登录入口，并把忘记密码移到登录动作附近。
- 将登录预览页的微信登录改为圆形居中的图标按钮，进一步压缩卡片内的视觉噪音。
- 登录卡片顶部新增 `邮箱验证码登录` 提示，并补充 `使用密码登录 >` 的蓝色切换入口。
- 登录页改成从上到下的三段式视觉顺序，登录卡片下移到聚光灯下方，不再垂直居中。
- 登录页进一步收敛为三元素整体居中堆叠，并统一登录页上下区域的背景色调。
- 登录页的聚光灯背景改为铺满全屏根容器，避免被品牌区高度裁切。
- 登录页聚光灯进一步收束为 `meeting` 下方的局部舞台光，减少对下方登录卡片的干扰。
- 登录态内的卡片切换统一保留在同一页壳层中，登录、登录后入口、预定会议和加入会议都只切换卡片状态，不会整页跳转。
- 登录态切到登录后入口时仅更新页面内卡片状态，不再改写 URL hash，减少跳页感。
- 正式前端登录页同步收敛为单页全屏壳层，登录、登录后入口、预定会议和加入会议卡片共享同一尺寸与背景，不再在状态切换时切回双栏布局；同时刷新了本地持久化键，避免旧的预填数据影响新布局。
- `meeting-auth-preview.html` 顶部增加了仅供预览使用的状态切换控制栏，可在登录、登录后入口、预定会议、加入会议和密码弹窗之间切换。
- 加入会议卡片精简为会议号和昵称输入，不再展示会议主题和状态预检摘要。
- `docs/design/meeting-auth-preview.html` 支持按视图状态定向渲染，`docs/design/render-previews.sh` 新增主持人视角的登录、创建会议和进入会议三张独立示意图输出。
- 后端默认 HTTP 监听端口改为 `5180`，前端本地开发代理同步切换到 `127.0.0.1:5180`，避免与本机已占用的 `8080` 端口冲突。
- 前端开发服务器默认端口改为 `5188`，避免与本机已占用的 `5173` 端口冲突。
- 前端主界面按最新 `docs/design` 重构为黑色风格产品化 UI，补齐登录壳层、登录后入口、预定会议 / 快速会议、两段式加入会议、会中主舞台、右侧成员 / 聊天抽屉和邀请 / 录制申请弹窗。
- 会中布局从旧版综合控制台切换为“黑色主舞台 + 右侧缩略窗 + 底部工具栏 + 下方辅助面板”的结构，并保留白板、就位确认、临时纪要、审计摘要等既有能力。
- 会中页面继续按 `docs/design/meeting-room-preview.html` 收敛为单屏全舞台布局，正式前端新增顶部标题栏、底部 dock 工具栏、主持人工具 / 会议工具双菜单，以及贴附式的设置 / 应用 / 结束会议子窗口。
- 会中页面继续补齐设计稿细节：顶部菜单、底部工具栏、应用 / 结束会议子窗口和昵称编辑入口统一改为 SVG 图标风格，并移除黑场舞台上的说明文案。
- 会中页顶栏信息继续收敛到设计稿：会议详情下方仅保留会议持续时间，并移除网络状态、分享和设置按钮点击后的常驻选中态。
- 会中页左上角网络状态入口改为 hover 浮层，鼠标移入后在按钮附近固定显示网络状态、延迟、丢包率和传输速率摘要，移出后自动消失。
- 会中空舞台移除中央说明文字和顶部 WSS 状态条，并把信令连接状态整合到左上角网络浮层中，同时保持参会者头像墙继续居中显示。
- 会中页分享窗口按最新设计稿重排为“会议号标题 + 左侧连续会议信息 + 右侧二维码”的结构，会议号复制入口改为贴着会议号显示，底部第二个按钮改为复制会议二维码，昵称编辑入口同步收紧为纯图标按钮。
- 会议系统新增公开的 `9` 位数字会议号，分享窗口、二维码、加入会议和扫码回填统一改用该会议号；内部 room id 继续仅用于会中运行态、审计和信令链路。
- 主持人登录页切换为测试模式，预填 `meeting@07c2.com.cn / helloworld`，并允许任意非空邮箱和密码直接登录，便于本地联调创建会议。
- 登录页继续按最新设计稿收敛为全屏单卡布局：登录态仅保留大号 `meeting`、下方聚焦光斑和居中的登录卡片，并统一为与会中主舞台一致的 macOS dark 风格底色。
- 中英文 `README` 首页新增登录界面宣传图，便于在仓库首页直接展示当前产品化登录视觉效果。
- 中英文 `README` 的 UI 流程描述已同步更新为当前事实，补齐全屏登录页和单屏会中布局的最新说明。
- Go 模块路径已对齐到当前公开仓库地址 `github.com/misterchenleiya/meeting`，避免外部拉取和引用模块时出现路径不匹配。
- `make build` 的产物路径调整为：后端输出到 `build/backend/meeting`，前端输出到 `build/frontend/`，便于区分前后端构建结果。
- 前端结构化日志改为仅保留浏览器控制台输出和当前页面内存中的最近日志，不再写入浏览器 `sessionStorage`；`warn`/`error` 和关键 `info` 事件会按批次上报到后端日志。
- 前端开发态 `WebSocket` 信令改为直接连接后端 `:5180`，不再经过 Vite 的 `/ws` 代理；主舞台头像墙仅展示自己和当前在线的参会者，减少断线残留对会中视图的干扰。
- 前端 RTC 链路改为可配置的 ICE / STUN / TURN 目标，并在信令断开后支持指数退避自动重连，减少弱网下手动刷新页面的需求。
- 前端审计统计开始驱动媒体质量档位，会根据延迟、丢包、码率和 peer 数自动调整视频上限，并在审计面板中展示当前档位和视频限制。
- 前端 Mesh 连接在 ICE 失败或断开时会自动触发重协商恢复，减少多人房间里个别 peer 短暂掉线后的人工介入。
- 登录入口拆分为独立的注册和登录路径，注册完成后会返回登录页并要求重新用验证码登录。
- 新增生产环境 Nginx 配置草案，并修正 `api.07c2.com.cn/meeting` 的反代规则为前缀剥离模式，确保后端收到的是原始 `/api/...` 与 `/ws/...` 路径。

### Fixed

- 后端日志输出统一为包含 `level`、`time`、`message` 的 JSON 格式，并将 `level` 调整为小写，满足项目全局日志规范。
- 后端日志文件补齐 24 小时轮转和最近 3 天保留策略，避免长时间运行时单文件持续增长且依赖重启才清理旧日志。
- 后端启动早期的日志初始化失败场景改为使用同一套 JSON 日志格式输出到标准错误，避免绕过项目日志规范。
- 修复登录页空闲时前端会因 `useEffectEvent` 被错误写入 `useEffect` 依赖而反复执行 RTC 清理，导致浏览器控制台持续刷出 `Maximum update depth exceeded`。
- 修复会中基础审计上报和入会扫码的前端 effect 依赖误用，避免主持人开会后审计日志以毫秒级频率刷屏，并消除同类扫码生命周期隐患。
- 修复从首页进入快速会议后页面可能保留原有滚动位置的问题，避免会中首屏落在黑色舞台中段时看起来像整页空白，进入和退出会议时会自动回到顶部。
- 修复主持人首次进入快速会议时信令连接可能在几十毫秒内被过期回调误关的问题，避免会中首屏出现整页黑场且必须手动刷新才能恢复。
- 修复部分浏览器环境缺少 `crypto.randomUUID()` 时快速会议首进流程会在本地事件写入阶段直接报错，避免主持人进入会议后出现整页黑场并只能靠刷新恢复。
- 修复会中点击摄像头、麦克风和屏幕共享按钮时对 `navigator.mediaDevices` 的兼容性判断缺失问题；在不支持或非安全上下文环境下不再直接抛前端异常，而是给出明确的浏览器与访问方式提示。
- 会议密码改为真正可选：快速会议和未设置密码的预约会议允许参会者仅凭会议号直接加入，不再强制弹出密码输入。
- 主持人结束会议前新增二次确认，结束后可先导出当前会议纪要，再返回预约会议或快速会议入口，降低误触成本。
- 前端会中刷新后会自动恢复当前会话并重连信令，不再直接回到登录页。
- 结束会议确认框改为按整个浏览器视口居中显示，不再跟随视频舞台区域定位，避免误以为点击结束会议没有反应。
- 修复主持人结束会议时的请求体字段不匹配问题，确认结束会议后不再被后端以 `invalid request body` 拒绝。
- 加入会议扫码在不支持 `getUserMedia` 的移动端环境下改为给出明确提示，并提供拍照 / 选图识别二维码的降级路径；图片识别会先缩放再解码，避免手机原图过大时看起来“没有反应”。
- 修复开发联调时 `WSS` 信令频繁断开的问题：前端改为直连后端信令地址，后端在参会者断开连接后会延迟清理并广播离会，避免关闭页面后人数持续累加。
